import { SidenavAccordianDirective } from './sidenav-accordian.directive';

describe('SidenavAccordianDirective', () => {
  it('should create an instance', () => {
    const directive = new SidenavAccordianDirective();
    expect(directive).toBeTruthy();
  });
});
